class stu(object):
	"""docstring for stu"""
	def __init__(self, name,score):
		self.name=name
		self.score=score
	def print_score(std):
		print '%s:%s' %(std.name,std.score)
p=stu('www',89)
print p.name
p.print_score()
